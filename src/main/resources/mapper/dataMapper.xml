<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team_ingterior.ingterior.mapper.TestMapper">
    
    <select id="selectTest" parameterType="integer" resultType="integer">
        select #{x}::integer + #{y}::integer
    </select>


</mapper>